<UserControl
    x:Class="game.title_page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    DataContext="{Binding view_model, RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <Button Content="Play" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20,10" Command="{Binding play_command}" />
    </Grid>
</UserControl>
